{
	"name": "typenet-workspace",
	"private": true,
	"workspaces": [
		"package",
		"playground"
	],
	"scripts": {
		"build": "cd ./playground && bun run build",
		"compile": "cd ./package && bun run compile && cd ../playground && bun run compile",
		"dev": "cd ./package && bun run watch & cd ./playground && bun run dev",
		"install": "find . -name \"node_modules\" -type d -prune -exec rm -rf {} + && bun i --ignore-scripts"
	},
	"packageManager": "bun@1.3.1",
	"dependencies": {
		"@rbxts/testez": "^0.4.2-ts.0"
	}
}
